import 'package:flutter/material.dart';

class CustomDosageView extends StatelessWidget {
  const CustomDosageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Custom dosage");
  }
}
